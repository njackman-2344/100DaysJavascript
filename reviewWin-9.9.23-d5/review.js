function form() {
  const inputTitle = document.getElementsByClassName("reviewTitle").textContent;
  const inputRI =
    document.getElementsByClassName("textReviewInput").textContent;
  const inputNickname = document.getElementsByClassName("nickname").textContent;
  const inputEmail = document.getElementsByClassName("email").textContent;
  const radio = document.getElementsByClassName("yes/no").value;

  const btn = document.getElementsByClassName("btnSubmit");
}
